import * as React from "react"
import "./CheckoutForm.css"

export default function CheckoutForm(isOpen, shoppingCart, quantity, checkoutForm, handleOnCheckoutFormChange, handleOnSubmitCheckoutForm) {
    
    return (
        <div className="checkout-form">
            CheckoutForm                                                                                
        </div>
    )
}